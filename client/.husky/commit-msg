if ! ./client/node_modules/.bin/commitlint --edit "$1" --config client/commitlint.config.js; then
  echo ""
  echo "Format attendu : <type>(<scope optionnel>): <description>"
  echo ""
  echo "Types valides : feat | fix | docs | style | refactor | test | chore | perf | ci | revert"
  echo ""
  echo "Exemples :"
  echo "  feat: ajouter la page lobby"
  echo "  fix(auth): corriger la validation du token"
  echo "  chore: mettre à jour les dépendances"
  echo ""
  exit 1
fi
